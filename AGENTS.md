# Development Agents Guide

This `AGENTS.md` is for developing `codex-handoff` itself.

It is not the runtime instruction block that `codex-handoff` writes into user
repositories. Runtime repo instructions are generated by the product and should
be treated separately from this development guide.

## Repository Focus

This repository builds the `codex-handoff` CLI, packaging flow, background
agent, watcher, and supporting documentation.

When working here, optimize for:

- clear repo-scoped sync behavior
- predictable install and upgrade lifecycle behavior
- background agent and watcher safety
- concise user-facing docs and prompt flows

## Memory Bootstrap

Before doing substantive work in this repository:

1. Read `.codex-handoff/latest.md` first if it exists.
2. If the user asks to continue previous work, run:
   `codex-handoff --repo . resume --goal "<user-goal>"`
3. If the bootstrap summary is insufficient, inspect:
   `.codex-handoff/handoff.json`
4. Never load raw session jsonl files wholesale. Use:
   `codex-handoff search`
   `codex-handoff extract`
   `codex-handoff context-pack`

## Development Rules

- Use `remote` as the product term for the synchronized backend.
- The first supported remote provider is Cloudflare R2.
- Treat `codex-handoff setup` as the primary repo bootstrap command.
- Treat `codex-handoff receive` as the cross-machine restore command.
- Keep package install behavior and repo setup behavior clearly separated in docs and code.
- During development before npm publish, verify packaging with:
  `npm pack`
  `npm install -g ./brdgkr-codex-handoff-<version>.tgz`
- Keep runtime lifecycle safe during npm install and upgrade:
  package install should stop running agent/watch processes before replacing files
  and restart the agent automatically afterward when needed.

## Docs Boundary

- Keep this file development-only.
- Do not add machine-specific paths, personal workflow notes, or attached-repo
  metadata here.
- User-facing runtime guidance belongs in:
  `skills/codex-handoff/SKILL.md`
  `docs/agent-install-prompts.md`
  `docs/agent-install-ux.md`

<!-- codex-handoff:start -->
## Codex Handoff Sync

This repository is attached to codex-handoff sync.
- local project name: `codex-handoff`
- local workspace root: `D:\source\repos\ideook\codex-handoff`
- remote auth: `global_dotenv`
- remote auth file: `~/.codex-handoff/.env.local`
- remote repo slug: `brdgkr-codex-handoff`
- remote prefix: `repos/brdgkr-codex-handoff/`
- summary mode: `auto`
- include raw threads: `false`
- match status: `existing_local`
- installed skill path: `C:\Users\DukhyunLee\.codex\skills\codex-handoff`

Before doing substantive work in this repository:
0. If the local `codex-handoff` skill is installed, use it for sync, pull, import, and resume orchestration.
1. Read `.codex-handoff/latest.md` first if it exists.
2. If the user asks to continue previous work, run `codex-handoff --repo . resume --goal "<user-goal>"`.
3. If the bootstrap summary is insufficient, inspect `.codex-handoff/handoff.json`.
4. Never load raw session jsonl files wholesale. Use `codex-handoff --repo . search`, `codex-handoff --repo . extract`, or `codex-handoff --repo . context-pack` to retrieve only relevant evidence.
<!-- codex-handoff:end -->
